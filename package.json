{
  "name": "dynamic-mcp",
  "version": "1.0.0",
  "description": "Dynamic MCP management system",
  "workspaces": [
    "client",
    "server", 
    "shared",
    "packages/*"
  ],
  "scripts": {
    "dev": "echo 'Choose: npm run dev:server or npm run dev:client'",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev:full": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "build": "npm run build:shared && npm run build:server && npm run build:client",
    "build:shared": "cd shared && npm run build",
    "build:server": "cd server && npm run build",
    "build:client": "cd client && npm run build",
    "install:all": "npm install",
    "clean": "npm run clean:shared && npm run clean:server && npm run clean:client",
    "clean:shared": "cd shared && npm run clean",
    "clean:server": "cd server && npm run clean || true",
    "clean:client": "cd client && npm run clean || true"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "pino-pretty": "^13.1.1",
    "prisma": "^6.13.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^6.13.0"
  }
}
