{
  "server": {
    "name": "memory-daemon",
    "version": "1.0.0",
    "description": "Memory management HTTP daemon service for storing and retrieving important information across conversations"
  },
  "tools": [
    {
      "name": "memory_remember",
      "description": "💾 REMEMBER information that should be recalled later. Use this to store facts, preferences, context, or any important information. Example: memory_remember with content='User lives in Berlin' and key='location'",
      "parameters": {
        "type": "object",
        "properties": {
          "content": {
            "type": "string",
            "description": "The information to remember. Be specific and detailed. Examples: 'User prefers dark mode', 'User lives in Berlin', 'Project uses React and TypeScript', 'User's favorite color is blue'",
            "minLength": 1
          },
          "key": {
            "type": "string",
            "description": "Category or tag for organizing memories. Examples: 'user-preferences', 'location', 'project-info', 'personal-facts', 'work-context'",
            "optional": true
          },
          "metadata": {
            "type": "object",
            "description": "Additional context like importance level, tags, expiration date, etc.",
            "optional": true
          },
          "userId": {
            "type": "number",
            "description": "User ID (optional, defaults to current user)",
            "optional": true
          }
        },
        "required": ["content"]
      }
    },
    {
      "name": "memory_recall",
      "description": "🧠 RECALL previously stored memories. Use this to retrieve information that was stored earlier. Search by content text or filter by key category.",
      "parameters": {
        "type": "object",
        "properties": {
          "key": {
            "type": "string",
            "description": "Find memories with specific category/tag. Examples: 'location', 'user-preferences', 'project-info'",
            "optional": true
          },
          "search": {
            "type": "string",
            "description": "Search for memories containing specific text. Examples: 'Berlin', 'dark mode', 'React', 'favorite color'",
            "optional": true
          },
          "limit": {
            "type": "number",
            "description": "Maximum number of memories to return (1-100, default 50)",
            "minimum": 1,
            "maximum": 100,
            "optional": true
          },
          "offset": {
            "type": "number",
            "description": "Number of memories to skip (for pagination, default 0)",
            "minimum": 0,
            "optional": true
          },
          "userId": {
            "type": "number",
            "description": "User ID (optional, defaults to current user)",
            "optional": true
          }
        }
      }
    },
    {
      "name": "memory_reset",
      "description": "🗑️ DELETE stored memories. Use with caution! Can delete all memories or just those with a specific key.",
      "parameters": {
        "type": "object",
        "properties": {
          "key": {
            "type": "string",
            "description": "Only delete memories with this specific key/category. If not provided, deletes ALL memories for the user!",
            "optional": true
          },
          "userId": {
            "type": "number",
            "description": "User ID (optional, defaults to current user)",
            "optional": true
          }
        }
      }
    }
  ],
  "resources": [
    {
      "uri": "memory://stats",
      "name": "Memory Statistics",
      "description": "Current memory usage statistics, categories, and stored information overview",
      "mimeType": "application/json"
    }
  ]
}
